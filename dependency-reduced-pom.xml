<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>SeleniumGenerator</groupId>
  <artifactId>SeleniumGenerator</artifactId>
  <version>1.0-SNAPSHOT</version>
  <build>
    <finalName>SeleniumGenerator</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>2.5.1</version>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <includeScope>runtime</includeScope>
              <outputDirectory>${project.build.directory}/dependency-jars/</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>2.1</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>Main</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <properties>
    <testSuiteXML>testng.xml</testSuiteXML>
    <surefire.version>2.20</surefire.version>
    <skipITs>${skipTests}</skipITs>
    <failsafe.version>2.20</failsafe.version>
    <maven.compiler.version>3.6.2</maven.compiler.version>
    <skipTests>false</skipTests>
    <testng.version>6.9.10</testng.version>
    <skipUTs>${skipTests}</skipUTs>
    <selenium.version>3.4.0</selenium.version>
  </properties>
</project>

